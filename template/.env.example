# Rowt Server Configuration
# Copy this file to .env and fill in your values

# ===== TENANT MODE =====
# 'single-tenant' or 'multi-tenant'
ROWT_TENANT_MODE='single-tenant'

# ===== DATABASE CONFIGURATION =====
# PostgreSQL (recommended for production)
ROWT_DATABASE_URL='postgresql://username:password@hostname:5432/database?sslmode=require'
ROWT_DB_TYPE='postgres'
ROWT_DB_SSL='true'

# OR SQLite (for development/testing)
# ROWT_DATABASE_URL='sqlite:rowt.sqlite'
# ROWT_DB_TYPE='sqlite'

# ===== SECURITY =====
# REQUIRED: Secret key for JWT tokens (use a long random string)
ROWT_JWT_SECRET='your-secret-key-here-change-this'

# ===== OPTIONAL: STRIPE (Multi-tenant mode only) =====
# Only needed if running in multi-tenant mode with paid tiers
# STRIPE_SECRET_KEY='sk_test_...'

# ===== SERVER =====
# Port to run the server on (default: 8080)
PORT=8080

# ===== SINGLE-TENANT MODE =====
# Required if ROWT_TENANT_MODE='single-tenant'
ROWT_ADMIN_EMAIL='admin@example.com'
ROWT_ADMIN_PASSWORD='your-secure-password'
ROWT_ADMIN_UUID='generate-a-uuid'
